<% provide :title, @entry.title.titleize %>
<div class="row">
  <aside class="col-md-4">
    <%= render "shared/user_info" %>
  </aside>
  <div class="col-md-8">
    <%= render "shared/entry" %>
    <hr>
    <%= render "comments/comment_form" if logged_in? %>
  </div>
</div>
